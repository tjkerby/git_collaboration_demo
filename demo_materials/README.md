# Prepared demonstration materials

These files keep the live coding fast and predictable.

- `issue-1-data-range.md` mirrors the issue created by the workflow.
- `issue-2-safe-average.md` is ready to paste into a new GitHub issue.
- `example-1-data-range.py` is the complete first proposal.
- `example-2-first-proposal.py` intentionally misses the empty case.
- `example-2-revision.py` is the revised second proposal.
- The three `conflict-*.py` files contain the two competing changes and their combined resolution.

